"""Interface gráfica mínima (placeholder) para integração futura."""

from __future__ import annotations


def launch_gui() -> None:
    """
    Lança uma GUI placeholder para demonstração.

    Esta função existe como ponto de extensão para uma interface real
    (ex.: Tkinter, PyQt ou frontend web).
    """
    print("GUI ainda não implementada. Usa src/interfaces/cli.py para jogar em modo terminal.")


if __name__ == "__main__":
    launch_gui()
